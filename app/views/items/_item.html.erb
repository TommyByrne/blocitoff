
  <div class="row bordered">
    <div class="col-md-10">
    <% if item.name!=nil %> <!-- Always had one extra check mark -->
    <div class="col-md-2"><%= link_to "", [item.list, item], method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %></div>
    <% end %>
    <p><%= item.name %></p>
  </div>
</div>
