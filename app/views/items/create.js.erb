<% if @item.valid? %>
  $('.js-items').prepend("<%= escape_javascript(render partial: 'items/item' %>");
    $('.new-item').html("<%= escape_javascript(render partial: 'items/form', locals: {list: @list, item: @new_item}) %>");
<% else %>
  $('.new-comment').html("<%= escape_javascript(render partial: 'items/form', locals: { list: @list, item: @item }) %>");
<% end %>